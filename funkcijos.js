$('#pirmas').on('change', function (e) {
    var optionSelected = $('option:selected', this);
    var valueSelected;
    valueSelected = this.value;
    console.log(valueSelected);
    $('[id="pozicija1"]').attr('src', valueSelected);
    });

$('#antras').on('change', function (e) {
    var optionSelected = $('option:selected', this);
    var valueSelected;
    valueSelected = this.value;
    console.log(valueSelected);
    $('[id="pozicija2"]').attr('src', valueSelected);
});
$('#trecias').on('change', function (e) {
    var optionSelected = $('option:selected', this);
    var valueSelected;
    valueSelected = this.value;
    console.log(valueSelected);
    $('[id="pozicija3"]').attr('src', valueSelected);
});
$('#ketvirtas').on('change', function (e) {
    var optionSelected = $('option:selected', this);
    var valueSelected;
    valueSelected = this.value;
    console.log(valueSelected);
    $('[id="pozicija4"]').attr('src', valueSelected);
});
$('#penktas').on('change', function (e) {
    var optionSelected = $('option:selected', this);
    var valueSelected;
    valueSelected = this.value;
    console.log(valueSelected);
    $('[id="pozicija5"]').attr('src', valueSelected);
});
$('#sestas').on('change', function (e) {
    var optionSelected = $('option:selected', this);
    var valueSelected;
    valueSelected = this.value;
    console.log(valueSelected);
    $('[id="pozicija6"]').attr('src', valueSelected);
});
$('#septintas').on('change', function (e) {
    var optionSelected = $('option:selected', this);
    var valueSelected;
    valueSelected = this.value;
    console.log(valueSelected);
    $('[id="pozicija7"]').attr('src', valueSelected);
});
$('#astuntas').on('change', function (e) {
    var optionSelected = $('option:selected', this);
    var valueSelected;
    valueSelected = this.value;
    console.log(valueSelected);
    $('[id="pozicija8"]').attr('src', valueSelected);
});
$('#devintas').on('change', function (e) {
    var optionSelected = $('option:selected', this);
    var valueSelected;
    valueSelected = this.value;
    console.log(valueSelected);
    $('[id="pozicija9"]').attr('src', valueSelected);
});
$('#desimtas').on('change', function (e) {
    var optionSelected = $('option:selected', this);
    var valueSelected;
    valueSelected = this.value;
    console.log(valueSelected);
    $('[id="pozicija10"]').attr('src', valueSelected);
});
$('#vienuoliktas').on('change', function (e) {
    var optionSelected = $('option:selected', this);
    var valueSelected;
    valueSelected = this.value;
    console.log(valueSelected);
    $('[id="pozicija11"]').attr('src', valueSelected);
});
$('#dvyliktas').on('change', function (e) {
    var optionSelected = $('option:selected', this);
    var valueSelected;
    valueSelected = this.value;
    console.log(valueSelected);
    $('[id="pozicija12"]').attr('src', valueSelected);
});
$('#tryliktas').on('change', function (e) {
    var optionSelected = $('option:selected', this);
    var valueSelected;
    valueSelected = this.value;
    console.log(valueSelected);
    $('[id="pozicija13"]').attr('src', valueSelected);
});
$('#keturioliktas').on('change', function (e) {
    var optionSelected = $('option:selected', this);
    var valueSelected;
    valueSelected = this.value;
    console.log(valueSelected);
    $('[id="pozicija14"]').attr('src', valueSelected);
});
$('#penkioliktas').on('change', function (e) {
    var optionSelected = $('option:selected', this);
    var valueSelected;
    valueSelected = this.value;
    console.log(valueSelected);
    $('[id="pozicija15"]').attr('src', valueSelected);
});
$('#sesioliktas').on('change', function (e) {
    var optionSelected = $('option:selected', this);
    var valueSelected;
    valueSelected = this.value;
    console.log(valueSelected);
    $('[id="pozicija16"]').attr('src', valueSelected);
});
$('#septynioliktas').on('change', function (e) {
    var optionSelected = $('option:selected', this);
    var valueSelected;
    valueSelected = this.value;
    console.log(valueSelected);
    $('[id="pozicija17"]').attr('src', valueSelected);
});
$('#astuonioliktas').on('change', function (e) {
    var optionSelected = $('option:selected', this);
    var valueSelected;
    valueSelected = this.value;
    console.log(valueSelected);
    $('[id="pozicija18"]').attr('src', valueSelected);
});
$('#visi').on('change', function (e) {
    var optionSelected = $('option:selected', this);
    var valueSelected;
    valueSelected = this.value;
    console.log(valueSelected);
    $('[id="pozicija1"]').attr('src', valueSelected);
    $('[id="pozicija2"]').attr('src', valueSelected);
    $('[id="pozicija3"]').attr('src', valueSelected);
    $('[id="pozicija4"]').attr('src', valueSelected);
    $('[id="pozicija5"]').attr('src', valueSelected);
    $('[id="pozicija6"]').attr('src', valueSelected);
    $('[id="pozicija7"]').attr('src', valueSelected);
    $('[id="pozicija8"]').attr('src', valueSelected);
    $('[id="pozicija9"]').attr('src', valueSelected);
    $('[id="pozicija10"]').attr('src', valueSelected);
    $('[id="pozicija11"]').attr('src', valueSelected);
    $('[id="pozicija12"]').attr('src', valueSelected);
    $('[id="pozicija13"]').attr('src', valueSelected);
    $('[id="pozicija14"]').attr('src', valueSelected);
    $('[id="pozicija15"]').attr('src', valueSelected);
    $('[id="pozicija16"]').attr('src', valueSelected);
    $('[id="pozicija17"]').attr('src', valueSelected);
    $('[id="pozicija18"]').attr('src', valueSelected);
});